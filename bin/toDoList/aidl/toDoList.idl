module toDoList{
	interface toDoList{
		string register(in string name,in string password);
		string logIn(in string name,in string password);
		string addItem(in long long startTime,in long long endTime,in string label);
		string queryItems(in long long startTime,in long long endTime);
		string deleteItem(in long index);
		string listAll();
		string logOut();
		string clearItems();
	};
};